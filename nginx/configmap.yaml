apiVersion: v1
kind: ConfigMap
metadata:
  name: ingress-nginx-controller
  namespace: ingress-nginx
data:
  # Enable underscores in headers - required for Chatwoot api_access_token header
  # Without this, nginx drops headers with underscores causing 401 Unauthorized errors
  enable-underscores-in-headers: "true"
